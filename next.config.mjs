/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "summerfield.store",
      "store.storeimages.cdn-apple.com",
      "i.dummyjson.com",
      "drive.google.com",
    ],
  },
};

export default nextConfig;
